@foreach($teams as $komanda)
<div>{{print_r($komanda)}} </div>
@endforeach

@foreach($projects as $projektas)
<div>{{print_r($projektas)}} </div>
@endforeach



@foreach($images as $image)
<div>{{print_r($image)}} </div>
@endforeach

<div>{{print_r($region)}} </div>

<div>{{print_r($plans)}} </div>

